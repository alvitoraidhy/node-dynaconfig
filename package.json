{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "rimraf packages/*/coverage packages/*/lib",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test:all": "jest --coverage",
    "test:ci": "npm run test:all && codecov"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^15.12.5",
    "codecov": "^3.8.2",
    "jest": "^27.0.6",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.0.3",
    "tsc": "^2.0.3",
    "typescript": "^4.3.4"
  }
}
