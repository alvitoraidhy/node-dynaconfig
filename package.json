{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "npm run bootstrap",
    "clean": "rimraf ./packages/*/coverage ./packages/*/lib",
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run lint",
    "format": "lerna run format",
    "build": "lerna run build",
    "test": "lerna run test",
    "test:ci": "npm-run-all bootstrap build test",
    "stage": "npm-run-all bootstrap lint format build test",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}
